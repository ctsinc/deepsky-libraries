import 'package:flutter/material.dart';

class DSWIcon extends StatelessWidget{
  const DSWIcon({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }

}